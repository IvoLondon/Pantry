import React from 'react';
import ReactDOM from 'react-dom';

import App from './src/app';

const rootElement = document.getElementById('root');
ReactDOM.render(
    <App />,
    rootElement
);
// TODO: USE <React.StrictMode> </React.StrictMode>;
